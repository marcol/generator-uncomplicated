{
  "name": "<%= project %>",
  "version": "0.1.0",
  "description": "Check out my new project: <%= project %>",
  "keywords": [],
  "homepage": "",
  "bugs": "",
  "repository": {
    "type": "",
    "url": ""
  },
  "license": "ISC",
  "author": "<%= name %> <<%= email %>>",
  "scripts": {
    "lint": "npm run lint:js && npm run lint:md && npm run lint:css",
    "lint:css": "stylelint **/*.html **/*.css; exit 0",
    "lint:js": "eslint .; exit 0",
    "lint:md": "remark .; exit 0",
    "serve": "npm run start",
    "start": "<% if (hasWebpack) { %>webpack-dev-server --open --config webpack.dev.js<% } else { %>static-server --port 8080 source/<% } %>",
    "build": "<% if (hasWebpack) { %>webpack --config webpack.prod.js<% } %>",
    "test": "<% if (hasMocha) { %>mocha 'test/index.js'<% } %>"
  },
  "dependencies": {},
  "devDependencies": {}
}
